Components are the building blocks of a React UI. Each component is a function that returns JSX.

```jsx
function Button({ label }) {
  return <button>{label}</button>;
}
```

export const title = "Components & Props";
